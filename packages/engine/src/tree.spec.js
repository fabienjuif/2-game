describe('trees', () => {
  it('should summon a tree (only one) next to an other each turn', () => {
  })

  it('should not summon a tree on a not empty cell', () => {

  })

  it('should not summon a tree if there is not tree around him', () => {

  })

  it('should let a villager remove a tree', () => {

  })

  it('should let a soldier remove a tree', () => {

  })

  it('should let a king remove a tree', () => {

  })
})
