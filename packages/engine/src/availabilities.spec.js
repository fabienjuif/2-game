describe('availabilities', () => {
  it('should not let a villager attack a house', () => {

  })

  it('should let a villager attack a cell that is surrounded by something < at him', () => {

  })

  it('should not let a villager attack a cell that is surrounded by something > at him', () => {

  })

  it('should not let a unit be played twice a turn', () => {

  })

  it('should let a unit be played once a turn', () => {

  })

  it('should not let a house be moved', () => {

  })
})
